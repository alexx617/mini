export function test(data) {
    return {
        method: 'post',
        url: '/api/users/login',
        data
    }

    // getUserInfo(userId){
    //     if(!userId)
    //     return this.get('/ddddd', {},{header}).then().catch()
    // }
}